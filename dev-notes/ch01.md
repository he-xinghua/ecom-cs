# 第一章开发留痕 — 电商智能客服系统（纯对话链路）

> 记录规则：每完成一个阶段（brainstorm 定稿、计划评审通过、每个任务完成、code review 结论、finish）就追加一段，**不攒到最后一次性补记**。
> 每段固定记四样：① 本阶段关键原话 ② 关键产出 ③ 拒绝/纠偏 ④ 翻车与返工。

---

## 阶段 0：环境探查与项目定位

**① 关键原话**
> 「我要用 Superpowers 模式做一个电商智能客服系统，第一步先跑通纯对话。」

**② 关键产出**
- 定位到目标仓库：`D:\Hxh\Coding\ecom-cs`（当日新建的空 git 仓库，`master` 分支，零 commit）
- 环境实测：Python **3.14.6**（`C:\Users\Hxh\AppData\Local\Python\pythoncore-3.14-64`），全局干净，无 fastapi/langchain/openai；PATH 内无 uv、无 conda
- 依赖可解析性实测（`pip install --dry-run`）：fastapi 0.141.1 / langchain **1.4.0** / langchain-openai 1.6.2 / langchain-core 1.6.3 —— **Python 3.14 全部兼容，无需降版本**

**③ 拒绝/纠偏**
- 会话启动时的 cwd 是 `C:\Windows\System32`（系统目录，不可写入）。经探查确认 `ecom-cs` 空仓库才是目标，**未在 System32 下建任何文件**。

**④ 翻车与返工**
- `pip install --dry-run --report` 连续失败三次才拿到结果：
  1. `--report -`（输出到 stdout）被 pip 内置 rich 在 GBK 控制台渲染时崩掉
  2. 改写到 `/tmp/piprep.json` —— Git Bash 的 `/tmp` 与 Python 的路径视图不一致，文件没落到预期位置
  3. 改用 Windows 绝对路径后，`json.load` 又因中文 Windows 默认 GBK 编码读取 UTF-8 文件而报 `UnicodeDecodeError`，显式指定 `encoding='utf-8'` 才通过
- 另注：pip 联网频繁出现 `SSL: UNEXPECTED_EOF_WHILE_READING` 重试。**这是贯穿本章的环境风险，安装依赖需按镜像重试。**

---

## 阶段 1：brainstorm 定稿

**① 关键原话**
> 「全程走 Superpowers 流程，技能自动触发；产出不是可单测代码的任务（纯 Prompt、数据类），把 TDD 那步换成拿标注样例或评估集跑一遍验证，其余步骤照走；聊天页面是例外，用 Vibe Coding 方式直接做……不套 brainstorm、TDD、code review 那套流程」
> 「涉及具体库、框架、API 的用法……一律先用 Context7 MCP 查最新官方文档和接口定义再动手，别凭记忆写，版本对不上的 API 是返工重灾区」
> 「上面点名的技术选型是定死的，实现中发现矛盾或走不通，停下来问我，不要自行换方案」

**② 关键产出**
- Spec：`docs/superpowers/specs/2026-09-15-ecom-cs-ch01-design.md`（已自审：修掉「confidence 语义」与「expected_resolution 判定口径」两处歧义）
- 分类：**architectural 路径**（新项目，非 bounded）
- 通过 Context7 + WebSearch 核实的接口事实（均为实测/官方文档，非记忆）：
  - `trim_messages` 完整签名；**`include_system` 默认 `False`**；`token_counter` 接受字面量 `"approximate"`
  - LangChain 已是 **1.x**，主线为 `init_chat_model` / `create_agent` / middleware；0.x 的 `ConversationBufferMemory` 等写法已不在主线
  - `ChatOpenAI` + 自定义 `base_url` 的官方警告：只保证官方 OpenAI 规范，代理特有字段可能不被保留

**③ 拒绝/纠偏**
用户四次拍板，其中一次是对**我提出的技术矛盾**的纠偏：

| 问题 | 用户选择 | 被否决的选项 |
|---|---|---|
| 会话历史归谁存 | **服务端内存表** | 无状态客户端传全量；SQLite 落盘 |
| 结构化抽取落地形态 | **独立端点 `/extract`** | 对话流内旁路自动抽取；两者都做 |
| 聊天页面是否进 ch01 | **不进，延后到后续章节** | ch01 末尾顺带做极简页；页面优先做 |
| **结构化输出跨 provider 矛盾** | **方案 A：method 按 provider 配置化** | 统一 `function_calling`；原生 SDK 分家 |

**最重要的一次纠偏**：我原设计把「用 `with_structured_output`」当成直白需求，核实后发现它与「四家模型统一走 OpenAI 协议」**存在真实矛盾**——没有任何单一 method 在四家上都有 schema 保证，且失败是**静默**的（不报错，只给错 JSON）。按用户「工作要求 4」，停下来提问而非自行换方案。用户拍板 A：不换选型，加一层薄的能力表 + Pydantic 校验 + 重试兜底。

### 附：执行模式决策（阶段 1 内）

**问题**：用户问「检查是否存在 Subagent-Driven」。核实 `superpowers:subagent-driven-development` 存在且适用（有实施计划 ✅ / 任务基本独立 ✅ / 留在当前会话 ✅）。但读其定义后发现它与用户三条明文要求冲突，列出后交用户拍板。

**用户决策**：

| 冲突 | 用户裁定 |
|---|---|
| SDD 明文禁止中途停下问人（"Rulings, not stalls"）vs 用户「工作要求 4：发现矛盾停下来问我」 | **用户规则优先——遇事就停下问**。依据 `using-superpowers` 的优先级：用户指令 > 技能 |
| SDD 要求 worktree 隔离 vs 现状（`ch01-pure-chat` 分支，非 master，非 worktree） | 待定，已单独提问 |
| SDD 的 implementer 模板内建 TDD 期待 vs 用户「纯 Prompt/数据类用评估集替代 TDD」 | 派子代理时按用户规则改写模板（不再单独提问，属用户已明示的范围） |

**其余记录**：
- 执行阶段将由我协调、子代理实现，**我不写业务代码**（控制器改代码会绕过评审）
- SDD 的 ledger（`.superpowers/sdd/<plan>/progress.md`，gitignore）与 `dev-notes/ch01.md` **是两套记录**：ledger 供我防上下文丢失，dev-notes 供用户看过程。用户要求的四要素仍记在 dev-notes。

**④ 翻车与返工**
- **设计初稿被自己推翻**：初稿写「system 必须独立保留在裁剪之外」，但未意识到 `trim_messages` 的 `include_system` **默认是关闭的**。若不显式传 `True`，客服 System Prompt 会被整条裁掉，且不报任何错。这类「默认值反直觉 + 静默失败」已两次出现（另一次是 provider 的 method），列为本章重点防范模式。
- **`token_counter` 的选型修正**：原本想传模型对象做 token 计数，实测文档后确认那会走 tiktoken——而 tiktoken 对 DeepSeek / Claude / Ollama 的分词不适用，token 预算控制会失真。改用手面量 `"approximate"`，四家口径统一。
- 环境限制记录：本仓库**未安装 `elements-of-style` 技能**，spec 为手写，未调用该技能。

**⑤ 本阶段遗留的实测项（不凭记忆断言，进实现前必须验证）**
1. `ChatOpenAI.with_structured_output` 在 LangChain **1.4** 的默认 `method` 究竟是什么
2. Anthropic OpenAI 兼容层对 `function_calling` 的实际可用性（文档称 `strict` 被忽略但基础调用可用）
3. Ollama OpenAI 兼容端点对 `response_format` 的实际处理（官方声明与社区 issue 报告不一致）

---

## 阶段 2：远程接入与隔离工作区（会话交接点）

**① 关键原话**
> 「先加远程，再走完整流程」
> （提供 URL：`git@github.com:he-xinghua/ecom-cs.git`）

**② 关键产出**
- 远程已接入：`origin` → `github.com:he-xinghua/ecom-cs.git`（用户自建空仓库）
- `master` 分支已建立并推送，为远程默认分支；`origin/HEAD` 已用 `remote set-head -a` 显式指向 `master`
- 已推送 commit：`93bae24`（spec + dev-notes）→ `fde623b`（SDD 决策补记）
- 实测通过的凭证：SSH 双通道可用 —— Gitee `Xinghuahh(@xinghuahh)`、GitHub `he-xinghua`

**③ 拒绝/纠偏**
- 原本本地只有 `ch01-pure-chat` 分支（`master` 处于"未出生"零 commit 状态）。**刻意把 spec 那个 commit 移到 `master`**，理由：spec 与留痕属项目基线，不属功能分支；且 `worktree.baseRef=fresh` 需要 `origin/<默认分支>` 存在。已删除冗余的本地 `ch01-pure-chat`。
- 用户否掉了「不开 worktree」与「手工 worktree」两个选项，坚持走完整原生流程。

**④ 翻车与返工**
- **检测脚本假阳性（已自查纠正）**：初次跑隔离检测时，`git -C` 返回相对路径 `.git`，`cd` 从 System32 出发解析失败，`GIT_DIR`/`GIT_COMMON` 双双为空字符串。两个空串相等，于是判定语句**碰巧**输出正确结论。已重跑并改用 `--path-format=absolute`，得到真实值 `D:/Hxh/Coding/ecom-cs/.git`，确认非 worktree。**教训：结论正确不等于依据成立，测试坏了必须重跑。**
- **`origin/HEAD` 缺失**：默认未设置，会让 `baseRef=fresh` 找不到基准分支。已补设。
- **`EnterWorktree` 被拒**：报错 `not in a git repository and no WorktreeCreate hooks`。根因不在 worktree，而在**会话 cwd = `C:\Windows\System32`（非 git 仓库）**。这会导致：原生 worktree 工具不可用、worktree 不被 harness 管理、SDD 的 `scripts/sdd-workspace` / `task-brief` / `review-package` 按 repo-root 解析路径时会算错、记忆目录被键在 `C--Windows-System32`。
- 用户裁定 **A：重启 Claude Code 到项目目录**。本段即为交接记录。

**⑤ 交接给下一个会话（重要）**

状态：**spec 已定稿并经用户评审通过，尚未开始实施。** 下一步是 `superpowers:writing-plans` 产出实施计划，随后按 SDD 执行。

重启后请先读：
1. `CLAUDE.md`（项目硬规则与进度指针）
2. `docs/superpowers/specs/2026-09-15-ecom-cs-ch01-design.md`（binding authority）
3. 本文件（过程留痕，按阶段追记，**不许收尾时一次性补记**）
